export enum HttpStatusCode {
    OK = 200,
    CREATED = 201,
    NO_CONTENT = 204,

    BAD_REQUEST = 400,
    NOT_AUTHENTICATED = 401,
    NOT_FOUND = 404,
    CONFLICT = 409,
    FORBIDDEN = 403,
}